# shellcheck shell=sh
# Non-secret defaults that Codex agents can source for this repository.

# Ensure Codex uses the shared reproducible setup script by default.
export CODEX_SETUP="${CODEX_SETUP:-./scripts/setup.sh}"

# Prefer the CI-oriented Foundry profile for deterministic builds.
export FOUNDRY_PROFILE="${FOUNDRY_PROFILE:-ci}"
